## Useful commands

- `npm install [package] --workspace=[app / package name]` For installing packages in specific apps / packages

- `npm run build` Builds everything!

- `docker compose up -d --build` Rebuilds images and starts containers!
